# Node.js API container — talks to PostgreSQL over the Docker network.
FROM node:20-alpine

WORKDIR /app

# Install dependencies first (layer caching: only re-runs when package.json changes).
COPY package.json ./
RUN npm install

COPY server.js db.js init.sql ./

EXPOSE 3000

CMD ["npm", "start"]
